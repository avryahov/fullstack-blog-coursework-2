- name: Run post-deploy smoke checks
  hosts: app_servers
  become: true
  gather_facts: true
  vars_files:
    - ../inventories/production/group_vars/app_servers.yml

  roles:
    - role: smoke_checks
